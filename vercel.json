{
	"version": 2,
	"builds": [
		{
			"src": "apps/api/main.ts",
			"use": "@vercel/node",
			"config": {
				"distDir": "dist/apps/api"
			}
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "src/main.ts",
			"methods": ["GET", "POST", "PUT", "DELETE"]
		}
	]
}
