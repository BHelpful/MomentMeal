{
	"version": 2,
	"builds": [
		{
			"src": "package.json",
			"use": "@vercel/angular",
			"config": {
				"distDir": "dist/apps/meal-time"
			}
		},
		{
			"src": "apps/api/main.ts",
			"use": "@vercel/node",
			"config": {
				"distDir": "dist/apps/api"
			}
		}
	],
	"routes": [
		{
			"src": "/api/(.*)",
			"dest": "/api/$1"
		},
		{
			"src": "/(.*)",
			"dest": "/$1"
		}
	]
}
