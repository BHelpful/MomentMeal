describe('JwtStrategy', () => {
	it('should be true', () => {
		expect(true).toBe(true);
	});
	// TODO: Fix this test
	it('should be defined', () => {
		// expect(new JwtStrategy()).toBeDefined();
	});
});
